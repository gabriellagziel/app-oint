targets:
  $default:
    builders:
      json_serializable:
        options:
          explicit_to_json: true
        generate_for:
          include:
            - lib/features/**/models/*.dart
      freezed:
        generate_for:
          include:
            - lib/features/**/models/*.dart 